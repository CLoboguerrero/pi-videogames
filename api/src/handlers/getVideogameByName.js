const axios = require('axios');
const { Videogame } = require('../db');
const { API_KEY } = process.env;

const getVideogameByName = async (req, res) => {
    try {
        
    } catch (error) {
        
    }
}

modules.exports = { getVideogameByName }