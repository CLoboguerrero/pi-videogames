const axios = require('axios');
const { Videogame, Genre } = require('../db');
const { API_KEY } = process.env;

const getGenres = async (req, res) => {
    try {
        
    } catch (error) {
        
    }
}

modules.exports = { getGenres }